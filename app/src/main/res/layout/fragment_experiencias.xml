<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/beige_claro"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">


        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <ImageView
                android:id="@+id/imgLogo"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_centerInParent="true"
                android:src="@drawable/logo"
                android:contentDescription="Logo" />
        </RelativeLayout>


        <TextView
            android:id="@+id/txtTitulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MI EXPERIENCIA"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/beige_claro"
            android:layout_gravity="start"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="32dp"
            android:background="@drawable/bg_titulo_experiencia"
            android:paddingStart="24dp"
            android:paddingEnd="36dp"
            android:paddingTop="14dp"
            android:paddingBottom="14dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:paddingBottom="32dp">


            <Button
                android:id="@+id/btnGrabarVideo"
                android:layout_width="220dp"
                android:layout_height="48dp"
                android:text="Grabar video"
                android:textColor="@color/texto_oscuro"
                android:textSize="15sp"
                android:layout_gravity="end"
                android:background="@drawable/bg_btn_grabar"
                android:layout_marginBottom="12dp"
                android:stateListAnimator="@null" />


            <FrameLayout
                android:id="@+id/layoutVideoPreview"
                android:layout_width="match_parent"
                android:layout_height="220dp"
                android:layout_marginBottom="20dp"
                android:visibility="gone">

                <!-- PreviewView: muestra la cámara EN VIVO mientras se graba
                     Se oculta cuando la grabación termina -->
                <androidx.camera.view.PreviewView
                    android:id="@+id/previewCamera"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone" />

                <!-- VideoView: reproduce el video YA GRABADO
                     Se muestra solo cuando la grabación terminó exitosamente -->
                <VideoView
                    android:id="@+id/videoViewPreview"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone" />


                <ImageButton
                    android:id="@+id/btnQuitarVideo"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_gravity="top|end"
                    android:layout_margin="6dp"
                    android:src="@drawable/ic_close"
                    android:background="@drawable/bg_btn_publicar"

                    android:contentDescription="Quitar video"
                    android:padding="4dp" />
            </FrameLayout>

            <EditText
                android:id="@+id/etComentario"
                android:layout_width="match_parent"
                android:layout_height="160dp"
                android:hint="Escriba un comentario..."
                android:gravity="top|start"
                android:background="@drawable/bg_edittext"
                android:padding="16dp"
                android:textColor="@color/texto_oscuro"
                android:textColorHint="#B0A090"
                android:textSize="14sp"
                android:inputType="textMultiLine"
                android:layout_marginBottom="32dp" />

            <Button
                android:id="@+id/btnPublicar"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:text="Publicar"
                android:textColor="@color/beige_claro"
                android:textSize="17sp"
                android:textStyle="bold"
                android:background="@drawable/bg_btn_publicar"
                android:stateListAnimator="@null" />

        </LinearLayout>
    </LinearLayout>
</ScrollView>